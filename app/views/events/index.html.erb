<h1>ALL EVENTS</h1>


<% @events.each do |event| %>
  <p>
    <%= event.name %> |
    <%= link_to 'View', event_path(event) %> |
    <%= link_to 'Edit', edit_event_path(event) %> |
    <%= link_to 'Delete', event_path(event), method: :delete, data: {confirm: 'You Sure Bro?'} %> |
  </p>
<% end %>